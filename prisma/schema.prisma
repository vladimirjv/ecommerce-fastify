generator client {
  provider = "prisma-client-js"
  // previewFeatures = ["mongoDb"]
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Product {
  id                   Int                    @id @default(autoincrement())
  name                 String                 @db.VarChar(255)
  description          String
  price                Decimal                @db.Money
  available            Boolean                @default(true)
  created_at           DateTime               @default(now()) @db.Date
  updated_at           DateTime               @updatedAt @db.Date
  CategoriesOnProducts CategoriesOnProducts[]
}

// id          String     @id @default(auto()) @map("_id") @db.ObjectId // MONGODB
// categoryIDs String[]   @db.ObjectId // MONGODB
// categories  Category[] @relation(fields: [categoryIDs], references: [id]) // MONGODB

model Category {
  // id          String    @id @default(auto()) @map("_id") @db.ObjectId
  id                   Int                    @id @default(autoincrement())
  name                 String                 @db.VarChar(255)
  created_at           DateTime               @default(now()) @db.Date
  updated_at           DateTime?              @updatedAt @db.Date
  CategoriesOnProducts CategoriesOnProducts[]
}

model CategoriesOnProducts {
  product    Product  @relation(fields: [productId], references: [id])
  productId  Int
  category   Category @relation(fields: [categoryId], references: [id])
  categoryId Int
  assignedAt DateTime @default(now())
  assignedBy Int


  @@id([productId, categoryId])
}

// productsIDs String[]  @db.ObjectId
// products    Product[] @relation(fields: [productsIDs], references: [id])
